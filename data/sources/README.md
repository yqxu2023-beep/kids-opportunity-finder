# Source uploads

Place future provider imports in `sources_upload.csv` with these columns:

`provider_name,website_url,facebook_url,perfectmind_url,contact_email,source_type,check_frequency,active,notes`

The initial source records are maintained in `supabase/seed-sources.sql`.
